/**
 * Author: SkyCodr (aka: Dulan Sudasinghe)
 * Date: 01.12.2019
 */

const taskSync = require('./task-sync');
const taskAsync = require('./task-async');
const taskWrangledAsync = require('./task-wrangled-async');

module.exports = { taskSync, taskAsync, taskWrangledAsync };
